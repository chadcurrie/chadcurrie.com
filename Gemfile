source "https://rubygems.org"

# Plain, modern Jekyll for local preview. GitHub Pages builds the live
# site server-side on its own, independent of this file, so this only
# needs to run on this machine — it doesn't need to match GitHub exactly.
gem "jekyll"
gem "webrick"
